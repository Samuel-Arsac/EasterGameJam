using UnityEngine;

public class Moulin : Structures
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
     public Moulin() 
         :base("Moulin", 2, 60, 10000, 10000000){
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
