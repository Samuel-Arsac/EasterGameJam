using UnityEngine;

public class Cuisine : Structures
{
    public Cuisine()
        : base("Cuisine",2,120,1000000,1000000000)
    {

    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
